/* 1 */
SELECT P.*, T.NOME, T.ENDERECO, T.TELEFONE FROM PACIENTE p 
LEFT JOIN  TUTOR T ON T.ID_TUTOR = P.ID_TUTOR

/* 2 */
SELECT P.*, T.* FROM PACIENTE p
INNER JOIN TUTOR t ON T.ID_TUTOR = P.ID_TUTOR

/* 3 */
SELECT P.*, T.* FROM TUTOR t
LEFT JOIN PACIENTE p ON T.ID_TUTOR = P.ID_TUTOR

/* 4 */
SELECT P.*, T.* FROM TUTOR t
INNER JOIN PACIENTE P ON P.ID_TUTOR = T.ID_TUTOR

/* 5 */
SELECT M.NOME, I.*, R.DATA_EMISSAO FROM ITENSRECEITA i 
LEFT JOIN RECEITA r
ON I.ID_RECEITA = R.ID_RECEITA
LEFT JOIN MEDICAMENTOS m
ON I.ID_MEDICAMENTO = M.ID_MEDICAMENTO

/* 6 */
SELECT M.NOME, I.*, R.DATA_EMISSAO FROM ITENSRECEITA i 
INNER JOIN RECEITA r
ON I.ID_RECEITA = R.ID_RECEITA
INNER JOIN MEDICAMENTOS m
ON I.ID_MEDICAMENTO = M.ID_MEDICAMENTO

/* 7 */
SELECT R.*, P.ESPECIE, P.NOME FROM RECEITA r
LEFT JOIN AGENDAMENTO A
ON A.ID_AGENDAMENTO = R.ID_AGENDAMENTO
LEFT JOIN PACIENTE P
ON P.ID_PACIENTE = A.ID_PACIENTE

/* 8 */
SELECT R.*, P.* FROM RECEITA r
INNER JOIN AGENDAMENTO A
ON A.ID_AGENDAMENTO = R.ID_AGENDAMENTO
INNER JOIN PACIENTE P
ON P.ID_PACIENTE = A.ID_PACIENTE

/* 9 */
SELECT R.*, M.*, I.DOSAGEM FROM RECEITA R 
LEFT JOIN ITENSRECEITA I
ON I.ID_RECEITA = R.ID_RECEITA
LEFT JOIN MEDICAMENTOS M
ON M.ID_MEDICAMENTO = I.ID_MEDICAMENTO

/* 10 */
SELECT R.*, M.*, I.DOSAGEM, I.QUANTIDADE FROM RECEITA R 
INNER JOIN ITENSRECEITA I
ON I.ID_RECEITA = R.ID_RECEITA
INNER JOIN MEDICAMENTOS M
ON M.ID_MEDICAMENTO = I.ID_MEDICAMENTO